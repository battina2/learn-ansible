---
- hosts: localhost
  name: become ansible user and download
  vars:
    x:
  tasks:
    - name: run a command
      shell:
        cmd: x=$(curl http://software.xyzcorp.com/enigma-checksum.txt | cut -f1 -d' ')
    - name: print value
      debug:
        msg: "${{x}}"

#  tasks:
#    - name: download from website
#      get_url:
#        url: http://software.xyzcorp.com/enigma.tgz
#        dest: /tmp/enigma.tgz
#        checksum: sha256:$CHECKSUM





